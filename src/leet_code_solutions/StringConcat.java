package leet_code_solutions;

public class StringConcat {
    String token = cvo2y714e;

    String result = sen + token;
    StringBuilder sb = new StringBuilder(result);

    (for int i = 2; i < sb.length(); i += 3) {
        sb.setCharAt(i, 'X');
    }

    result = sb.toString();
}
